# MCP Server Environment Configuration
# Copy this file to .env and update with your actual values
# Version: 1.0.0

# ============================================================================
# REMOTE SERVER CONFIGURATION
# ============================================================================

# SSH connection to remote server (format: user@hostname_or_ip)
EBON_HOST=ebon@10.0.0.29

# SSH authentication (choose ONE method):

# METHOD 1 (RECOMMENDED): SSH Key Authentication
# Path to your SSH private key file
# If set, this will be used instead of password authentication
# Generate with: ssh-keygen -t ed25519 -C "skippy@system"
# Copy to server with: ssh-copy-id user@server
SSH_KEY_PATH=

# METHOD 2 (NOT RECOMMENDED): Password Authentication
# SSH password for remote server authentication
# WARNING: Password visible in process list when using sshpass
# Only use this if SSH keys are not an option
EBON_PASSWORD=

# SSH connection options
# accept-new: Accept new host keys but reject changed keys (prevents MITM attacks)
# StrictHostKeyChecking=no is INSECURE and should never be used in production
SSH_OPTS=-o StrictHostKeyChecking=accept-new -o UserKnownHostsFile=/dev/null

# ============================================================================
# PATH CONFIGURATION
# ============================================================================

# Base directory for Skippy system files
# This should be an absolute path on your system
SKIPPY_BASE_PATH=/home/dave/skippy

# WordPress installation directory
# Used for WordPress management operations
WORDPRESS_BASE_PATH=/home/dave/RunDaveRun

# Skippy conversations directory
# Defaults to {SKIPPY_BASE_PATH}/conversations if not set
SKIPPY_CONVERSATIONS_PATH=/home/dave/skippy/conversations

# Skippy scripts directory
# Defaults to {SKIPPY_BASE_PATH}/scripts if not set
SKIPPY_SCRIPTS_PATH=/home/dave/skippy/scripts

# WordPress backup directory
# Defaults to {WORDPRESS_BASE_PATH}/backups if not set
WORDPRESS_BACKUP_PATH=/home/dave/RunDaveRun/backups

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Log level for the MCP server
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
SKIPPY_LOG_LEVEL=INFO

# ============================================================================
# MCP SERVER CONFIGURATION
# ============================================================================

# Timeout for MCP tool operations (in seconds)
# Default: 30
MCP_TOOL_TIMEOUT=30

# Maximum file size for read operations (in MB)
# Default: 100
MCP_MAX_FILE_SIZE_MB=100

# Maximum search results to return
# Default: 100
MCP_MAX_SEARCH_RESULTS=100

# ============================================================================
# SECURITY SETTINGS
# ============================================================================

# Enable input validation (STRONGLY RECOMMENDED)
# Set to 'false' only for debugging purposes
# Default: true
SKIPPY_ENABLE_VALIDATION=true

# Allowed base directories for file operations (comma-separated)
# If set, file operations are restricted to these directories
# Example: /home/dave/skippy,/home/dave/RunDaveRun
# Default: empty (no restriction)
SKIPPY_ALLOWED_BASE_DIRS=

# ============================================================================
# DATABASE CONFIGURATION (for WordPress operations)
# ============================================================================

# MySQL/MariaDB connection settings for WordPress database
# Used by mysql_query_safe tool
WORDPRESS_DB_HOST=localhost
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress_readonly
WORDPRESS_DB_PASSWORD=

# ============================================================================
# PERFORMANCE SETTINGS
# ============================================================================

# Enable file operation caching
# Default: false
MCP_ENABLE_CACHE=false

# Cache TTL in seconds
# Default: 300 (5 minutes)
MCP_CACHE_TTL=300

# ============================================================================
# DEVELOPMENT SETTINGS
# ============================================================================

# Enable debug mode (more verbose logging)
# Default: false
MCP_DEBUG_MODE=false

# Enable performance profiling
# Default: false
MCP_ENABLE_PROFILING=false
